[project]
name = "agentrouter-proxy"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27,<1",
    "anthropic>=0.40,<1",
]
